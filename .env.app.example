# =============================================================================
# WebGL SPA - Deployment Configuration
# =============================================================================
# Copy this file to .env.app and fill in your values:
#   cp .env.app.example .env.app
# =============================================================================

# -----------------------------------------------------------------------------
# Remote Server Configuration
# -----------------------------------------------------------------------------

# SSH connection string (user@host or user@ip)
REMOTE_HOST=

# Path to SSH private key
SSH_KEY_PATH=~/.ssh/id_rsa

# -----------------------------------------------------------------------------
# Deployment Paths
# -----------------------------------------------------------------------------

# Remote directory for WebGL SPA deployment
REMOTE_DIR=/opt/webgl-spa

# -----------------------------------------------------------------------------
# Docker Configuration
# -----------------------------------------------------------------------------

# Docker network to join (must exist on remote, created by gateway stack)
NETWORK_NAME=shared-services

# Container name
CONTAINER_NAME=webgl-spa

# Image name (used for build and transfer)
IMAGE_NAME=webgl-spa

# -----------------------------------------------------------------------------
# Domain Configuration
# -----------------------------------------------------------------------------

# Domain where the app will be accessible
DOMAIN=

# Path where the app is served (should match svelte.config.js paths.base)
APP_PATH=/spa
